# Copy to .env and fill in. Never commit the real .env.

# DB — use the Session pooler host (the direct db.<ref>.supabase.co host is deprecated).
DATABASE_URL=postgresql://compliance_intern.ruuufhxrubeckbcvwvcq:YOUR_PASSWORD@aws-1-us-east-1.pooler.supabase.com:5432/postgres

# api.data.gov family keys — only needed for the PLANNED federal expansion
# (Regulations.gov dockets + Congress.gov statutes). eCFR + dir.ca.gov need no key.
DATA_GOV_API_KEY=your_api_data_gov_key
CONGRESS_API_KEY=your_congress_gov_key
